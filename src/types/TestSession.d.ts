export interface TestSessionData {
  id: string;
  testSessionId: string;
  timeStamp: string;
  project: string;
  build: string;
  script: string;
  device: string;
  os: string;
  location: string;
  network: string;
}
