import { Flex } from '@chakra-ui/layout';
import React from 'react';

function SubTabWrapper({ children }) {
  return <div style={{ padding: '10px', background: 'white' }}>{children}</div>;
}

export default SubTabWrapper;
{
}
